<snippet>
	<content><![CDATA[use Workerman\Worker;

\$worker = new Worker('${1:websocket://0.0.0.0:8080}');
\$worker->count = 4;

\$worker->onMessage = function (\$connection, \$data) {
    \$connection->send('Processed by worker ' . \$connection->worker->id);
};

Worker::runAll();]]></content>
	<scope>source.php</scope>
	<tabTrigger>wkworkerid</tabTrigger>
	<description>Workerman Worker ID</description>
</snippet>
