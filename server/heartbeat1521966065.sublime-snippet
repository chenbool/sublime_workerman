<snippet>
	<content><![CDATA[use Workerman\Worker;

\$worker = new Worker('${1:websocket://0.0.0.0:8080}');

\$worker->onMessage = function (\$connection, \$data) {
    \$connection->send(json_encode(['type' => 'pong', 'data' => \$data]));
};

Worker::runAll();]]></content>
	<scope>source.php</scope>
	<tabTrigger>wkwsheart</tabTrigger>
	<description>Workerman WebSocket 心跳</description>
</snippet>
